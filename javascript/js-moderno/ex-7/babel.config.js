module.exports = {
    presets: [
        ["@babel/preset-env"]
    ]
}

//no package.json na chave "babel", a parte do valor "--presets=@babel/preset-env" pode ser omitida por conta da configuração acima.